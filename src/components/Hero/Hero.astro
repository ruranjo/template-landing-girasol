---
import HeroImage from "../../assets/hero-image.webp"; 
import IconLaptop from "../../assets/laptop.svg"; 
import IconMarketing from "../../assets/marketing.svg"; 
---

<section id="hero" class="bg-gray-50 px-6 py-16 lg:py-24" aria-label="Hero de presentación de Leonela, Community Manager">
  <div class="max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
    
    <!-- Columna izquierda -->
    <div class="text-center lg:text-left space-y-5">
      <p class="text-green-700 text-lg font-semibold">👋 Hola, soy Leonela</p>

      <h1 class="text-3xl sm:text-4xl lg:text-5xl font-extrabold leading-snug text-gray-900">
        Tu Community Manager
      </h1>

      <p class="mt-3 text-base sm:text-lg text-gray-700 leading-relaxed">
        Construyo <strong>conexiones auténticas</strong> y <strong>comunidades comprometidas</strong>, generando resultados reales para tu marca 🚀
      </p>

      <a 
        id="whatsapp-link"
        aria-label="Contactar a Leonela por WhatsApp"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 mt-6 text-white bg-green-700 border-2 border-black px-5 sm:px-7 py-2 sm:py-3 rounded-xl shadow-[4px_4px_0px_black] text-base sm:text-lg font-semibold hover:translate-x-[2px] hover:translate-y-[2px] hover:shadow-none transition-all"
      >
        <!-- SVG WhatsApp -->
        Hablemos
      </a>
    </div>

    <!-- Columna derecha -->
    <div class="relative flex justify-center gap-4">
      <img src={HeroImage.src} alt="Foto de Leonela, Community Manager" class="relative z-20 rounded-lg shadow-xl w-48 sm:w-72 lg:w-96 transition duration-300 hover:shadow-2xl hover:scale-[1.02]" loading="lazy" />

      <img src={IconLaptop.src} alt="Icono representando laptop" class="absolute top-2 sm:top-4 left-8 sm:left-12 w-12 sm:w-20 h-12 sm:h-20 bg-pink-300 rounded-xl p-1 sm:p-2 shadow-md animate-float-slow z-30" />

      <img src={IconMarketing.src} alt="Icono representando marketing digital" class="absolute bottom-4 sm:bottom-6 right-8 sm:right-12 w-12 sm:w-16 h-12 sm:h-16 bg-yellow-300 rounded-full p-2 sm:p-3 shadow-md animate-float z-30" />

      <div class="absolute -bottom-8 sm:-bottom-10 left-1/2 -translate-x-1/2 bg-green-300 border-2 border-black rounded-md px-3 sm:px-5 py-1 sm:py-2 shadow-[3px_3px_0px_black] flex items-center gap-4 sm:gap-8 animate-float-slow z-30">
        <span class="text-xs sm:text-sm font-semibold">👋 ¡Encantada de conocerte!</span>
      </div>
    </div>
  </div>
</section>

<script is:inline>
  const countryCode = "54"; // Argentina
  const number = "92954814701"; // número oculto
  const message = encodeURIComponent("Hola, quiero saber más sobre tus servicios");

  const btn = document.getElementById("whatsapp-link");
  if (btn) {
    btn.href = `https://wa.me/${countryCode}${number}?text=${message}`;
  }
</script>

<style>
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-12px); }
}
.animate-float { animation: float 4s ease-in-out infinite; }
.animate-float-slow { animation: float 6s ease-in-out infinite; }
</style>
